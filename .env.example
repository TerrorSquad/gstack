# Local Supabase: `pnpm supabase start` prints these values.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_KEY=your-anon-or-publishable-key

# Service role key bypasses RLS — server-side only. Read by scripts/ (seed) and
# e2e directly from process.env.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Same service_role key, consumed by the Nuxt app at runtime via
# serverSupabaseServiceRole() (health probe, notification webhook). On Vercel it
# MUST use the NUXT_ prefix to reach the serverless runtime.
NUXT_SUPABASE_SECRET_KEY=your-service-role-key

# --- Optional: everything below is off unless set ---

# Distributed rate limiting (Upstash Redis; see docs/adr/0003-upstash-redis.md).
# Falls back to an in-memory limiter when unset. Create at https://upstash.com.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# CSRF (nuxt-csurf). Works out of the box; set this to a fixed random string so
# tokens stay valid across deploys (unset = a fresh secret per build, which
# invalidates in-flight tokens on redeploy). Generate: openssl rand -hex 32
NUXT_CSURF_ENCRYPT_SECRET=

# Social login (login page). Enable the provider in supabase/config.toml, set
# these, and restart local Supabase. Create OAuth apps at:
#   GitHub  -> Settings -> Developer settings -> OAuth Apps
#   Google  -> Google Cloud Console -> Credentials
# Callback URL: http://127.0.0.1:54321/auth/v1/callback (local).
SUPABASE_AUTH_EXTERNAL_GITHUB_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GITHUB_SECRET=
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=

# Sentry: DSN is public-safe (write-only ingest key). Also set org/project in
# nuxt.config.ts and SENTRY_AUTH_TOKEN here for source-map upload at build.
NUXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# BetterStack log forwarding (server-side). Both must be set or it no-ops.
NUXT_BETTERSTACK_SOURCE_TOKEN=
NUXT_BETTERSTACK_INGEST_URL=

# PostHog product analytics + feature flags (client-side). Off by default.
# Key is public-safe (write-only ingest). See layers/analytics.
NUXT_PUBLIC_POSTHOG_ENABLED=false
NUXT_PUBLIC_POSTHOG_KEY=
# Optional: defaults to https://us.i.posthog.com (use eu.i.posthog.com for EU).
NUXT_PUBLIC_POSTHOG_HOST=
# Optional: true to record session replays (privacy + cost — off by default).
NUXT_PUBLIC_POSTHOG_SESSION_REPLAY=

# Notifications subsystem (in-app bell + transactional email). Off by default.
NUXT_PUBLIC_NOTIFICATIONS_ENABLED=false
# Resend API key for transactional email (https://resend.com -> API Keys).
NUXT_RESEND_KEY=
# Shared secret the Supabase notifications DB webhook must send in x-webhook-secret.
# Generate with: openssl rand -hex 32
NUXT_NOTIFICATION_WEBHOOK_SECRET=
# Canonical public origin for email links (prod). Empty = use request origin.
NUXT_SITE_URL=

# Billing (Polar — Merchant of Record; see docs/adr/0001-payments-provider.md).
# Off until set. Create products/prices in the Polar dashboard, add a webhook to
# /api/hooks/billing (Standard Webhooks), and paste its signing secret here.
NUXT_PUBLIC_BILLING_ENABLED=false
NUXT_POLAR_ACCESS_TOKEN=
NUXT_POLAR_WEBHOOK_SECRET=
NUXT_POLAR_SERVER=sandbox
NUXT_POLAR_PRICE_PRO=
NUXT_POLAR_PRICE_ENTERPRISE=
